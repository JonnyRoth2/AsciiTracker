# Live video to ascii converter
Run python3 -m venv .venv && .venv/bin/pip install -r requirements.txt to get project requirements.

Run the server app locally then it should automatically link to whatever port you set it to 
(8765 by default) then go to the local page and will run the ascii tracking to your face.